events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # Logging
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;

    # 대용량 파일 업로드/다운로드 설정
    client_max_body_size 0;  # 무제한
    proxy_read_timeout 3600s;
    proxy_send_timeout 3600s;
    proxy_connect_timeout 60s;

    # Gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

    upstream frontend {
        server frontend:80;
    }

    upstream api {
        server api:8000;
    }

    upstream titiler {
        server titiler:80;
    }

    server {
        listen 80;
        server_name localhost;

        # Docker DNS resolver (MinIO 런타임 해석용 - upstream 없이도 동작)
        resolver 127.0.0.11 valid=30s ipv6=off;
        set $minio_backend http://minio:9000;

        # Frontend
        location / {
            proxy_pass http://frontend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
        }

        # API
        location /api/ {
            proxy_pass http://api;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # WebSocket support
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            
            # Timeouts for long-running requests
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
        }

        # Upload API (local chunk upload in local storage mode)
        # Disable buffering to reduce upload latency and improve sustained throughput.
        location /api/v1/upload/ {
            proxy_pass http://api;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_request_buffering off;
            proxy_buffering off;
            client_max_body_size 0;
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
            proxy_connect_timeout 60s;
        }

        # WebSocket endpoints
        location /ws/ {
            proxy_pass http://api;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_read_timeout 86400;
        }

        # tus Upload Server (DISABLED - using S3 Multipart Upload)
        # location /files/ {
        #     proxy_pass http://tusd;
        # }

        # TiTiler COG Tile Server
        location /titiler/ {
            # CORS for tile requests
            add_header 'Access-Control-Allow-Origin' '*' always;
            add_header 'Access-Control-Allow-Methods' 'GET, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'Accept, Content-Type' always;
            
            if ($request_method = 'OPTIONS') {
                add_header 'Content-Length' 0;
                add_header 'Content-Type' 'text/plain';
                return 204;
            }
            
            # Strip /titiler prefix when proxying
            rewrite ^/titiler/(.*) /$1 break;
            proxy_pass http://titiler;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            
            # Caching for tiles (1 hour)
            proxy_cache_valid 200 1h;
        }

        # Health check
        location /health {
            proxy_pass http://api/health;
            proxy_http_version 1.1;
        }

        # Offline Map Tiles (오프라인 타일맵)
        # 확장자 자동 감지: .jpg, .png 순서로 시도
        location /tiles/ {
            alias /data/tiles/;
            add_header Access-Control-Allow-Origin "*" always;
            # 확장자 없는 요청 시 jpg → png 순서로 시도, 없으면 빈 타일 반환
            try_files $uri $uri.jpg $uri.png @empty_tile;
            # 타일 캐시: 1일 (교체 시 Ctrl+Shift+R로 갱신 가능)
            expires 1d;
        }

        # 빈 타일 응답 (캐시하지 않음)
        location @empty_tile {
            add_header Access-Control-Allow-Origin "*" always;
            add_header Cache-Control "no-cache" always;
            return 204;
        }

        # MinIO Bucket Direct Access (for presigned URLs - thumbnails, ortho, etc.)
        location /aerial-survey/ {
            # Hide CORS headers from MinIO to prevent duplicates
            proxy_hide_header 'Access-Control-Allow-Origin';
            proxy_hide_header 'Access-Control-Allow-Methods';
            proxy_hide_header 'Access-Control-Allow-Headers';
            proxy_hide_header 'Access-Control-Expose-Headers';

            # Add CORS headers
            add_header 'Access-Control-Allow-Origin' '*' always;
            add_header 'Access-Control-Expose-Headers' 'ETag, x-amz-request-id, x-amz-id-2' always;

            proxy_pass $minio_backend;
            proxy_http_version 1.1;
            # Set Host to match the endpoint used for presigned URL generation (minio:9000)
            # This is critical for S3 V4 signature verification
            proxy_set_header Host minio:9000;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Caching for thumbnails
            proxy_cache_valid 200 1h;
        }

        # Block direct access to private uploads (require API auth)
        location /storage/images/ {
            return 403;
        }

        # Storage: 로컬 파일 우선, MinIO 폴백 (STORAGE_BACKEND에 따라 자동 전환)
        # 로컬 모드: /data/storage/ 에서 직접 서빙 (projects/ 만 공개)
        # MinIO 모드: @minio_storage 로 프록시
        location /storage/ {
            alias /data/storage/;
            add_header 'Access-Control-Allow-Origin' '*' always;
            expires 1h;
            try_files $uri @minio_storage;
        }

        # MinIO Storage Proxy (fallback for minio mode)
        location @minio_storage {
            # Hide CORS headers from MinIO to prevent duplicates
            proxy_hide_header 'Access-Control-Allow-Origin';
            proxy_hide_header 'Access-Control-Allow-Methods';
            proxy_hide_header 'Access-Control-Allow-Headers';
            proxy_hide_header 'Access-Control-Expose-Headers';

            # === CORS Configuration for S3 uploads ===
            # Handle preflight requests
            if ($request_method = 'OPTIONS') {
                add_header 'Access-Control-Allow-Origin' '*' always;
                add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, HEAD, OPTIONS' always;
                add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, Content-Length, X-Amz-Date, X-Amz-Content-Sha256, X-Amz-Security-Token' always;
                add_header 'Access-Control-Expose-Headers' 'ETag, x-amz-request-id, x-amz-id-2' always;
                add_header 'Access-Control-Max-Age' 86400 always;
                add_header 'Content-Length' 0;
                add_header 'Content-Type' 'text/plain';
                return 204;
            }

            # Add CORS headers to all responses (IMPORTANT: ETag needed for multipart)
            add_header 'Access-Control-Allow-Origin' '*' always;
            add_header 'Access-Control-Expose-Headers' 'ETag, x-amz-request-id, x-amz-id-2' always;

            # Rewrite to MinIO bucket path
            rewrite ^/storage/aerial-survey/(.*) /aerial-survey/$1 break;
            rewrite ^/storage/(.*) /aerial-survey/$1 break;

            proxy_pass $minio_backend;
            proxy_http_version 1.1;
            proxy_set_header Host minio:9000;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Disable buffering for large uploads
            proxy_request_buffering off;
            proxy_buffering off;

            # Large file upload timeouts
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
            client_max_body_size 0;
        }

        # S3 Multipart Upload Proxy (MinIO mode only - presigned URLs with S3 signatures)
        location /storage/aerial-survey/ {
            # Hide CORS headers from MinIO to prevent duplicates
            proxy_hide_header 'Access-Control-Allow-Origin';
            proxy_hide_header 'Access-Control-Allow-Methods';
            proxy_hide_header 'Access-Control-Allow-Headers';
            proxy_hide_header 'Access-Control-Expose-Headers';

            if ($request_method = 'OPTIONS') {
                add_header 'Access-Control-Allow-Origin' '*' always;
                add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, HEAD, OPTIONS' always;
                add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, Content-Length, X-Amz-Date, X-Amz-Content-Sha256, X-Amz-Security-Token' always;
                add_header 'Access-Control-Expose-Headers' 'ETag, x-amz-request-id, x-amz-id-2' always;
                add_header 'Access-Control-Max-Age' 86400 always;
                add_header 'Content-Length' 0;
                add_header 'Content-Type' 'text/plain';
                return 204;
            }

            add_header 'Access-Control-Allow-Origin' '*' always;
            add_header 'Access-Control-Expose-Headers' 'ETag, x-amz-request-id, x-amz-id-2' always;

            rewrite ^/storage/aerial-survey/(.*) /aerial-survey/$1 break;
            proxy_pass $minio_backend;
            proxy_http_version 1.1;
            proxy_set_header Host minio:9000;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_request_buffering off;
            proxy_buffering off;
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
            client_max_body_size 0;
        }
    }
}
